# Used by the OpenAI-compatible generation endpoint.
VLLM_API_KEY=replace-me

# Optional remote-training overrides. See llamafactory_pipeline/README.md.
# TRAIN_CONFIG=llamafactory_pipeline/server_config.yaml
# LLAMAFACTORY_PORT=8899
