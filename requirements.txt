# DP-RAG 后端 Python 依赖
# 安装: pip install -r requirements.txt
# 建议 Python >= 3.10
#
# 仅列项目直接使用的三方库。

# ── 核心 ──
pyyaml>=6.0
pydantic>=2.0
requests>=2.28
httpx>=0.24
numpy>=1.24

# ── 向量库 ──
pymilvus>=2.4.0

# ── API 服务 (FastAPI + Uvicorn) ──
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# ── Agentic / 专家研究模式 (LangGraph) ──
langgraph>=0.2.0
langchain-core>=0.2.0

# ── 可观测性 (可选; 缺失时 langgraph_metrics 自动降级) ──
prometheus_client>=0.20.0
